
### Installation

1. **Clone the Repository**

   ```bash
   create a python environment `python -m venv env`
   navigate to environment and activate it
   git clone https://github.com/dhananjayhegde07/Team_LOLO_Anvesana_Django.git
   navigate to the folder
   run `pip install -r requirements.txt`
   addtionally for local environment run django server using `manage.py runserver 0.0.0.0:8000`
   

   
